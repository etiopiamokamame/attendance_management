<% unless notice.blank? %>
  <div class="callout callout-info lead">
    <p><%= notice %></p>
  </div>
<% end %>
<% unless alert.blank? %>
  <div class="callout callout-danger lead">
    <p><%= alert %></p>
  </div>
<% end %>
